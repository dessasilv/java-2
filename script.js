var b = parseInt(window.prompt ('escreva um número') );
var a = parseInt(window.prompt ('escreva outro numero') );

if(a==b){
    document.write( "soma: ", a +b)
} else {
    document.write('multiplicação: ', a*b)
}
